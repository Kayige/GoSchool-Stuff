
/*
//slice example
func example() {
	var persons []Person
	var person Person
	var n int
	fmt.Println("Enter Number of Persons:")
	fmt.Scanln(&n)
	for i = 0; i < n; i++ {
		fmt.Println("Enter name:")
		fmt.Scanln(&person.Name)
		fmt.Println("Age:")
		fmt.Scanln(&person.age)
		persons = append(persons, person)
	}
	fmt.Println(persons)

	/*
	var n int
	fmt.Println("Enter Number of Persons:")
	fmt.Scanln(&n)
	for i = 0; i < n; i++ {
		fmt.Println("Enter name:")
		fmt.Scanln(&person.Name)
		fmt.Println("Age:")
		fmt.Scanln(&person.age)
		persons = append(persons, person)
	}
	fmt.Println(persons)
	*/
	
	/*
	map1 := make(map[string]int) // map[key]value
	map1["1weee"] = 1
	map1["2weee"] = 2 
	>> map1 {1weee: 1, 2weee: 2}
	*/
/*
catMap := make(map[string]string) */
